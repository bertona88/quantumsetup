name: Deploy Infinite Techno to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Validate generative audio instrument
        run: npm --prefix app run check

      - name: Stage static site
        run: |
          mkdir _site
          cp \
            app/index.html \
            app/styles.css \
            app/main.js \
            app/quantum-visual.js \
            app/spectrum-mountain.js \
            app/visual-grammar.js \
            app/causal-world.js \
            app/audio-engine.js \
            app/techno-model.js \
            app/material-planner.js \
            app/pulse-bass-timbres.js \
            app/emergent-form.js \
            app/generative-utils.js \
            app/trajectory-identity.js \
            app/moment-share.js \
            app/local-claims.js \
            app/track-dna.js \
            app/synth-genomes.js \
            app/synth-dsp.js \
            app/synth-worklet.js \
            app/taste-model.js \
            app/performance-controls.js \
            app/instrument-preview.js \
            app/signal-deck.js \
            app/manifest.webmanifest \
            app/sw.js \
            _site/
          cp -R app/icons _site/icons
          cp prototype/robots.txt _site/robots.txt

      - name: Upload static site
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
