{
  "name": "quantumsetup-visual-instrument",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A deterministic, quantum-inspired probability field visual instrument.",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test field-model.test.mjs",
    "check": "node --check field-model.js && node --check main.js && node --test field-model.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
